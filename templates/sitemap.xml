<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for loc in locs %}
   <url>
      <loc>{{site.BaseURL}}/{{ loc.Meta.Slug }}</loc> 
      <lastmod>{{ loc.Meta.lastmod }}</lastmod>
   </url>
   {% endfor %}
</urlset>